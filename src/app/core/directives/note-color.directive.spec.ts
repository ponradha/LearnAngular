import { NoteColorDirective } from './note-color.directive';

describe('NoteColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteColorDirective();
    expect(directive).toBeTruthy();
  });
});
